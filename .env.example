# OpenAI (optional — app works offline with a local parser if unset)
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.6-luna

# Live chat capability evals (npm run test:chat-capability)
# CHAT_EVAL_MODEL=gpt-5.6-luna
# CHAT_REASONING_EFFORT=max
# (none | minimal | low | medium | high | xhigh | max)
# CHAT_EVAL_FILTER=ict-overall
